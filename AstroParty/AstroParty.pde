  ArrayList Bullet;
  Ship [] ship;
  
  PShape rectangle;
  void setup() {
    ship = new Ship[4];
    ship[0] = new Ship(
    ship = new Ship();
    size(1000,1000);
    ship = new Ship();
  }

  void draw() {
    background(51);
    ship.update();
    if (keyPressed==true){
      ship.degree += 3;
    }
    
    void keyPressed(){
      for(Ship i: ship){
        if(key = i.keyLetters[0]){
          keys[0] = 
        
  }

  

    